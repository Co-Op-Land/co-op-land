# 🔔 실시간 알림 시스템 설계 및 리팩토링

## 📌 프로젝트 개요

알림은 다음 두 가지 방식으로 설계하였다:

- **RDB 기반 알림**
    - 로그인 후 확인 가능한 알림 목록
    - 읽음 처리 등 상태 저장
    - WebSocket 세션이 없는 유저를 위한 Fallback

- **푸시 기반 알림 (실시간)**
    - 브라우저 푸시를 활용한 UX 중심 알림
    - 실시간 반응성 제공

---
## ⚒️️ 기술 선택 이유

### ✅ Kafka
- 알림 트래픽: 분당 최대 10만 건의 알림 처리 필요
- 원래 RabbitMQ를 고려했지만, 메시지 큐 기반으로 큐 수 증가 시 스케일링 한계 존재
- Kafka는 파티션 기반 분산 처리로 수평 확장에 강하고 **Throughput**이 높아 적합
- 실시간 외에도 RDB 저장, 재처리 등의 확장 로직에 유리
- 🔧 **부하 테스트를 통해 최적의 파티션 전략 수립 예정**

---

### ✅ Redis Pub/Sub
- 실시간 브로드캐스트에 적합
- 구조가 단순하고, 딜레이가 거의 없음
- 개인적으로 Redis에 익숙해 빠르고 안정적인 구성 가능

---

### ✅ WebSocket (Spring)
- Spring에서 기본적으로 제공
- 과거에 사용해본 경험이 있어 비교적 쉽게 구현
- 익숙한 기술 스택 기반으로 빠른 개발 → 운영 및 확장도 고려된 선택

---
## ⚙️ 기본 아키텍처 구성

### 1. WebSocket 연결

- 클라이언트는 `/ws?token=Bearer...`로 WebSocket 연결
- 연결 시 유저의 WebSocket 세션이 서버에 등록됨

```java
Map<Long, Session> sessions = new ConcurrentHashMap<>();
```

---

### 2. 알림 이벤트 발행 (Kafka)

- 댓글 작성 시 `@TriggerNotification` 어노테이션을 통해 알림 이벤트 자동 트리거
- AOP 기반으로 Kafka로 메시지를 발행

---

### 3. Kafka → Redis Pub/Sub → WebSocket

- Kafka Consumer가 메시지를 수신
- Redis Pub/Sub 채널로 브로드캐스트
- 모든 인스턴스가 구독하여, 해당 유저가 연결되어 있다면 푸시 전송

---

## 💡 다중 클라이언트 & 인스턴스 환경 개선

### ✅ 다중 클라이언트 지원

기존 구조:

```java
Map<Long, Session> sessions; // 유저당 하나의 세션만 저장
```

문제점:

- PC에서 로그인 후 모바일 접속 시, 기존 세션이 끊김

해결:

```java
Map<Long, CopyOnWriteArrayList<Session>> sessions;
```

- 불변 리스트로 여러 세션 저장 가능
- 각 기기에서 동시에 같은 알림 수신 가능

---

### ✅ 다중 인스턴스 환경 지원

#### ❌ 잘못된 접근

- WebSocket 세션을 Redis에 저장하고, 모든 인스턴스에서 가져와 쓰는 방식 시도
- 실패 원인:
    - WebSocket 세션은 직렬화 불가능
    - TCP 연결 상태는 연결된 인스턴스 메모리에만 존재

#### 🟢 해결책: Redis Pub/Sub 기반 브로드캐스트

1. WebSocket 세션은 각 인스턴스 메모리에서만 관리
2. 알림 메시지는 Redis Pub/Sub 채널을 통해 모든 인스턴스에 브로드캐스트
3. 각 인스턴스는 수신한 메시지의 대상 유저가 연결되어 있는지 확인
    - 연결되어 있으면 WebSocket으로 푸시
    - 없으면 무시

이 방식은 다음 장점을 제공한다:

- 인스턴스 간 세션 공유 없이 수평 확장 가능
- Redis 채널을 활용한 간단한 브로드캐스트 구조
- 실시간성과 확장성 동시 확보

---

## 🧩 구조 리팩토링: 알림 유형 확장

### ✅ 기존 문제

- 알림 시스템은 `'댓글 알림'` 하나만을 기준으로 구현
- AOP 기반 구조는 다양한 알림 유형 확장에 한계  
  (추출 값, 수신자 수, RDB 저장 여부 등이 알림마다 다름)

---

### ✅ 전략 패턴 도입

- `NotificationStrategy` 인터페이스 정의
- 각 알림 유형을 전략 구현체로 분리
- 이벤트 객체 추출 및 Kafka 전송을 각 전략에서 처리

```java
public interface NotificationStrategy {
    NotificationEvent buildEvent(Object[] args, Object result);
}
```

#### 구현 예시

```java
public class CommentNotificationStrategy implements NotificationStrategy {
    public NotificationEvent buildEvent(Object[] args, Object result) {
        return new CommentNotificationEvent(...);
    }
}
```

- Producer는 전략을 찾아 해당 이벤트를 Kafka에 전송
- 새로운 전략 추가 시 `NotificationStrategy` 구현만 하면 됨

---

### ✅ 예외 처리, RDB 저장 등 공통 흐름 추상화

#### 공통 흐름은 default 메서드로 처리

```java
public interface NotificationStrategy {

    // AOP 이용하는 로직
    NotificationEvent buildEvent(Object[] args, Object result);

    // RDB 저장 로직, 실시간 알림(ex 게임 매칭 성공) 인 경우 오버라이딩만 수행
    void save(NotificationEvent event);
    
    default void execute(Object[] args, Object result) {
        // 전체 로직 수행 기본 로직
    }
}
```

---

### ✅ Producer의 실행 흐름

```java
@Around("@annotation(triggerNotification)")
public Object notify(ProceedingJoinPoint joinPoint, TriggerNotification triggerNotification) throws Throwable {
    Object result = joinPoint.proceed();
    Object[] args = joinPoint.getArgs();

    NotificationStrategy strategy = strategies.stream()
            .filter(s -> s.supports(triggerNotification.target()))
            .findFirst()
            .orElseThrow(() -> new InvalidRequestException(ErrorCode.INVALID_NOTIFICATION_TYPE));

    // 어노테이션의 target 변수에 따라 알맞은 전략 로직 실행
    strategy.execute(args, result);
    return result;
}
```
---

### ✅ 클래스 다이어그램
![전략-패턴-클래스-다이어그램.png](images/%EC%A0%84%EB%9E%B5-%ED%8C%A8%ED%84%B4-%ED%81%B4%EB%9E%98%EC%8A%A4-%EB%8B%A4%EC%9D%B4%EC%96%B4%EA%B7%B8%EB%9E%A8.png)

---

## 📦 알림 RDB 저장을 위한 일대다 관계 구현과 일급 컬렉션 적용

### ✉️ 배경

실시간 알림 시스템에서 수신자는 한 명일 수도 있고, 여러 명일 수도 있다.  
예를 들어:

- 댓글 알림 → 게시글 작성자 + 부모 댓글 작성자
- 평가 알림 → 평가 대상자 1명

따라서 하나의 알림(Notification)이 여러 수신자(NotificationRecipient)를 가질 수 있는 **1:N 관계**가 필요하다.  
이를 효율적으로 설계하고 유지보수성을 높이기 위해, **일급 컬렉션**을 함께 활용했다.

---

### 🧩 도메인 모델 설계

### 🔹 Notification

```java
@OneToMany(mappedBy = "notification", cascade = CascadeType.ALL, orphanRemoval = true)
private List<NotificationRecipient> toMemberIds = new ArrayList<>();
```

- 하나의 Notification은 여러 명의 수신자를 가질 수 있다.
- `cascade = ALL`로 함께 영속화되고, `orphanRemoval = true`로 참조 제거 시 삭제됨
- 알림 전송자(`fromMemberId`)와 대상(`target`, `relatedId`) 포함

```java
public void addRecipient(Long toMemberId) {
    NotificationRecipient recipient = NotificationRecipient.builder()
        .notification(this)
        .toMemberId(toMemberId)
        .build();
    this.toMemberIds.add(recipient);
}
```

### 🔹 NotificationRecipient

```java
@ManyToOne(fetch = FetchType.EAGER)
@JoinColumn(name = "notification_id")
private Notification notification;
```

- 알림 수신자 정보를 별도로 관리 (유저 ID, 읽음 여부 등)
- `@ManyToOne`으로 Notification과 연결

---

### 🧠 일급 컬렉션: `ToMemberIds`

`List<Long>` 기반의 수신자 ID 리스트를 감싸는 **일급 컬렉션**으로 다음 역할을 수행한다:

- 중복 제거 및 자기 자신 제외
- `Notification` 객체에 수신자 리스트를 일괄 추가

```java
public class ToMemberIds {
    private final List<Long> values = new ArrayList<>();
    private Long fromMemberId;

    public void add(Long memberId) {
        if (memberId != null && !Objects.equals(memberId, fromMemberId) && !values.contains(memberId)) {
            values.add(memberId);
        }
    }

    public void addTo(Notification notification) {
        values.forEach(notification::addRecipient);
    }
}
```

### ✅ 장점

- **도메인 용어로 의미를 부여**하여 가독성 증가
- 중복 제거, 예외 처리 등 **로직의 응집도 증가**
- 도메인 로직을 엔티티 외부에서 관리하여 **단일 책임 원칙 준수**

---

### ⚙️ 저장 흐름 예시

1. 알림 전략에서 `NotificationEvent`를 구성
2. `ToMemberIds.addTo(notification)` 호출로 수신자 등록
3. `notificationRepository.save(notification)` 호출 시 recipient 리스트도 함께 영속화됨

---

### ✅ 정리

- 다대일 관계 대신 **1:N 구조**로 알림 수신자 개별 관리
- 읽음 처리, 필터링 등의 로직 분리 용이
- `ToMemberIds` 일급 컬렉션을 통해 수신자 리스트를 안전하게 처리

> 알림 시스템에서 수신자 정보는 중요한 도메인 요소다.  
> 일급 컬렉션은 단순한 데이터 리스트 이상의 의미를 부여하며, 도메인 주도 설계의 좋은 실천 예시가 된다.


---
## ✨ 마무리

- WebSocket 기반 실시간 알림 시스템을 **다중 클라이언트, 다중 인스턴스 환경** 모두 대응 가능하도록 개선
- Kafka + Redis Pub/Sub + WebSocket 조합으로 **성능, 확장성, 실시간성** 확보
- 전략 패턴을 통해 알림 로직을 유연하게 확장할 수 있는 구조 설계 완료
- 수신자 관리를 위한 **1:N 관계와 일급 컬렉션**을 도입하여, 알림 저장 구조의 안정성과 확장성을 동시에 확보
- RDB 기반 알림 시스템과 실시간 푸시 알림의 **이중 전략 구조**를 통해 UX와 데이터 보존 요구를 모두 만족
---